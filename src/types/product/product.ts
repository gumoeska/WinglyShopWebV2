export type Product = {
    id: number,
    code: string,
    description: string,
    idCategory: number,
    categoryDescription: string,
    price: number,
    imageUrl: string,
    isActive: boolean,
    hasStock: boolean
}